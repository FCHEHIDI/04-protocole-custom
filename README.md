# 04 — Protocole Custom : Modbus RTU Bare-Metal Rust

<p align="center">
  <img src="jongleraveclestests.png" width="100%" alt="Fares on the Scottish Cliffside" />
</p>

---

```rust
#[test]
fn cinematic_moment() {
    let scene = Scene::new()
        .with_actor("Fares")
        .with_location("Scottish Cliffside")
        .with_props(vec!["Rust Test Files"])
        .with_effects(vec!["Mist", "Geysers", "Soft Light"]);

    scene.roll_camera();

    assert!(scene.is_iconic());
}
```

---

> Stack Modbus RTU `no_std` — 8 tests, 0 failures, bare-metal.  
> See [`protocole-custom/`](protocole-custom/) for the full implementation.
